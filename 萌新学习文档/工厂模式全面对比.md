# 工厂模式对比：维护性 vs 简洁性

## 📊 4 种工厂模式对比表

| 模式 | 复杂度 | 维护性 | 扩展性 | 代码量 | 性能 | 最佳场景 |
|------|--------|--------|--------|--------|------|----------|
| **Simple Factory** | ⭐ | ⭐⭐⭐⭐ | ⭐ | 少 | 最快 | 小型项目、格式种类少 |
| **Factory Method** | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ | 多 | 快 | 中型项目、需要继承 |
| **Abstract Factory** | ⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐⭐ | 很多 | 快 | 大型项目、多系列产品 |
| **Dictionary Registry** | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 中等 | 快 | 插件架构、动态注册 |

---

## 方案 1️⃣：Simple Factory（简单工厂）- ⭐ 最简单

### 代码
```csharp
// 简单工厂 - 一个类搞定
public static class MetadataFactory
{
    public static AIMetadata GetImageInfo(string imagePath)
    {
        var format = DetectFormat(imagePath);
        
        return format switch
        {
            "PNG" => PngMetadataExtractor.ReadAIMetadata(imagePath),
            "JPEG" => JpegMetadataExtractor.ReadAIMetadata(imagePath),
            "WEBP" => WebPMetadataExtractor.ReadAIMetadata(imagePath),
            _ => new AIMetadata()
        };
    }
    
    private static string DetectFormat(string path) => /* ... */;
}
```

### 优点
- ✅ **极其简单** - 只需要一个 switch 语句
- ✅ **代码量少** - 不到 20 行
- ✅ **性能最佳** - 直接 switch 分派
- ✅ **容易理解** - 新手一看就懂
- ✅ **编译期类型检查** - 不易出错

### 缺点
- ❌ **扩展性差** - 添加新格式需要修改此类
- ❌ **不符合 OCP** - 违反开放-闭合原则
- ❌ **测试困难** - 难以 Mock 各种格式

### 何时使用
- 🎯 项目规模小
- 🎯 格式种类固定（不会经常添加）
- 🎯 快速原型开发

### 维护成本
```
添加新格式 AVIF 的工作量：
1. 创建 AvifMetadataExtractor 类
2. 修改 MetadataFactory 中的 switch 语句
3. 重新编译
📊 总成本：中等（需要改动工厂类）
```

---

## 方案 2️⃣：Factory Method Pattern（工厂方法）- 推荐平衡方案

### 代码
```csharp
// 抽象工厂方法
public abstract class MetadataExtractor
{
    public abstract AIMetadata Read(string imagePath);
    public abstract void Write(string destPath, AIMetadata data);
    public abstract bool Verify(string destPath, AIMetadata data);
    
    // 工厂方法
    public static MetadataExtractor Create(string imagePath)
    {
        var format = DetectFormat(imagePath);
        return format switch
        {
            "PNG" => new PngExtractor(),
            "JPEG" => new JpegExtractor(),
            "WEBP" => new WebPExtractor(),
            _ => new NullExtractor()
        };
    }
}

// 具体实现
public class PngExtractor : MetadataExtractor
{
    public override AIMetadata Read(string imagePath) 
        => PngMetadataExtractor.ReadAIMetadata(imagePath);
    
    public override void Write(string destPath, AIMetadata data)
        => PngMetadataExtractor.WriteAIMetadata(destPath, data);
    
    public override bool Verify(string destPath, AIMetadata data)
        => PngMetadataExtractor.VerifyAIMetadata(destPath, data);
}

// 使用
var extractor = MetadataExtractor.Create(imagePath);
var metadata = extractor.Read(imagePath);
```

### 优点
- ✅ **易于扩展** - 添加新格式只需新建类
- ✅ **符合 OCP** - 对扩展开放，对修改关闭
- ✅ **便于测试** - 可以 Mock 任何提取器
- ✅ **易于维护** - 每个格式独立的类
- ✅ **支持多态** - 可以多态调用 Read/Write/Verify

### 缺点
- ⚠️ **代码量较多** - 需要抽象基类 + 具体实现类
- ⚠️ **稍微复杂** - 需要理解继承关系
- ⚠️ **性能稍差** - 多了虚方法调用

### 何时使用
- 🎯 中型项目
- 🎯 格式会逐步增加
- 🎯 需要统一接口处理各种格式

### 维护成本
```
添加新格式 AVIF 的工作量：
1. 创建 AvifMetadataExtractor 类
2. 创建 AvifExtractor : MetadataExtractor
3. 修改 Create() 方法的 switch 语句
📊 总成本：低（工厂类改动最小）
```

---

## 方案 3️⃣：Dictionary Registry Pattern - ⭐⭐⭐⭐⭐ 最灵活

### 代码
```csharp
public static class MetadataFactory
{
    // 全局注册表
    private static readonly Dictionary<string, Func<string, AIMetadata>> extractors = 
        new()
        {
            { "PNG", path => PngMetadataExtractor.ReadAIMetadata(path) },
            { "JPEG", path => JpegMetadataExtractor.ReadAIMetadata(path) },
            { "WEBP", path => WebPMetadataExtractor.ReadAIMetadata(path) }
        };

    // 核心工厂方法
    public static AIMetadata GetImageInfo(string imagePath)
    {
        var format = DetectFormat(imagePath);
        
        if (extractors.TryGetValue(format, out var extractor))
            return extractor(imagePath);
        
        return new AIMetadata();
    }

    // 【关键特性】运行时注册新格式
    public static void RegisterExtractor(string format, Func<string, AIMetadata> extractor)
    {
        extractors[format] = extractor;
    }

    // 【关键特性】运行时注销格式
    public static void UnregisterExtractor(string format)
    {
        extractors.Remove(format);
    }

    // 【关键特性】查询已支持的格式
    public static IEnumerable<string> GetSupportedFormats() 
        => extractors.Keys;
}

// 使用
MetadataFactory.GetImageInfo("photo.png");  // 使用预注册的提取器

// 运行时注册新格式（如插件）
MetadataFactory.RegisterExtractor("AVIF", 
    path => AvifMetadataExtractor.ReadAIMetadata(path));

// 查询支持的格式
foreach (var format in MetadataFactory.GetSupportedFormats())
{
    Console.WriteLine($"支持的格式: {format}");
}
```

### 优点
- ✅ **最易扩展** - 无需修改工厂类代码
- ✅ **运行时注册** - 支持插件架构
- ✅ **最灵活** - 可动态加载/卸载格式
- ✅ **易于测试** - 可在测试中注册 Mock
- ✅ **符合 DIP** - 完全依赖接口

### 缺点
- ⚠️ **代码量中等** - 需要注册逻辑
- ⚠️ **编译期无法检查** - 格式检查延迟到运行时
- ⚠️ **性能稍差** - Dictionary 查询比 switch 慢 1-2%

### 何时使用
- 🎯 大型项目
- 🎯 需要插件支持
- 🎯 格式频繁变动
- 🎯 需要运行时配置

### 维护成本
```
添加新格式 AVIF 的工作量：
方案 A - 修改工厂类：
  1. 在 extractors Dictionary 中添加一行
  2. 重新编译
  
方案 B - 配置文件注册：
  1. 在 config.json 中配置 AVIF 处理器
  2. 启动时自动加载，无需重新编译
  
📊 总成本：最低（甚至可以不改代码！）
```

---

## 方案 4️⃣：Abstract Factory Pattern - 最复杂

### 代码
```csharp
// 产品族
public interface IMetadataExtractor
{
    AIMetadata Read(string path);
}

public interface IMetadataWriter
{
    void Write(string path, AIMetadata data);
}

public interface IMetadataValidator
{
    bool Verify(string path, AIMetadata data);
}

// 抽象工厂
public abstract class MetadataFactory
{
    public abstract IMetadataExtractor CreateExtractor();
    public abstract IMetadataWriter CreateWriter();
    public abstract IMetadataValidator CreateValidator();
    
    public static MetadataFactory Create(string format)
    {
        return format.ToUpper() switch
        {
            "PNG" => new PngMetadataFactory(),
            "JPEG" => new JpegMetadataFactory(),
            "WEBP" => new WebPMetadataFactory(),
            _ => throw new NotSupportedException()
        };
    }
}

// 具体工厂 - PNG 系列
public class PngMetadataFactory : MetadataFactory
{
    public override IMetadataExtractor CreateExtractor() 
        => new PngExtractor();
    
    public override IMetadataWriter CreateWriter() 
        => new PngWriter();
    
    public override IMetadataValidator CreateValidator() 
        => new PngValidator();
}

// 具体工厂 - JPEG 系列
public class JpegMetadataFactory : MetadataFactory
{
    public override IMetadataExtractor CreateExtractor() 
        => new JpegExtractor();
    
    public override IMetadataWriter CreateWriter() 
        => new JpegWriter();
    
    public override IMetadataValidator CreateValidator() 
        => new JpegValidator();
}

// 使用
var factory = MetadataFactory.Create("PNG");
var extractor = factory.CreateExtractor();
var writer = factory.CreateWriter();
var validator = factory.CreateValidator();

var metadata = extractor.Read("photo.png");
writer.Write("output.png", metadata);
validator.Verify("output.png", metadata);
```

### 优点
- ✅ **结构最清晰** - 明确的产品族关系
- ✅ **完全符合 SOLID** - 所有原则都满足
- ✅ **易于维护** - 相关类组织在一起

### 缺点
- ❌ **最复杂** - 代码量大
- ❌ **学习成本高** - 需要理解抽象工厂
- ❌ **过度设计** - 对小项目可能是浪费

### 何时使用
- 🎯 大型企业项目
- 🎯 需要完整的产品族
- 🎯 Read/Write/Verify 需要一起变化

### 维护成本
```
添加新格式 AVIF 的工作量：
1. 创建 AvifExtractor 类
2. 创建 AvifWriter 类
3. 创建 AvifValidator 类
4. 创建 AvifMetadataFactory 类
5. 修改 MetadataFactory.Create() 的 switch
📊 总成本：最高（需要创建 4 个新类）
```

---

## 🏆 建议总结

### 按场景推荐排序

#### 小型项目（< 5 个格式）
```
推荐度排序：
1️⃣  Simple Factory（简单工厂）
    └─ 理由：快速开发，维护简单
2️⃣  Dictionary Registry
    └─ 理由：以防后续需要扩展
```

#### 中型项目（5-20 个格式）
```
推荐度排序：
1️⃣  Factory Method（工厂方法）⭐ 最平衡
    └─ 理由：易扩展，代码清晰，不过度设计
2️⃣  Dictionary Registry
    └─ 理由：如果格式会频繁增加
```

#### 大型项目（> 20 个格式，或需要插件）
```
推荐度排序：
1️⃣  Dictionary Registry ⭐ 最灵活
    └─ 理由：支持运行时注册，适合插件架构
2️⃣  Abstract Factory
    └─ 理由：需要完整产品族关系
```

---

## 💡 你的项目应该选哪个？

### 当前状态（3 个格式：PNG、JPEG、WebP）
**最适合：Simple Factory**

```csharp
// ✅ 完全够用
public static class MetadataFactory
{
    public static AIMetadata GetImageInfo(string imagePath)
    {
        var format = ImageTypeDetector.DetectImageFormat(imagePath);
        return format switch
        {
            ImageTypeDetector.ImageFormat.PNG => 
                PngMetadataExtractor.ReadAIMetadata(imagePath),
            ImageTypeDetector.ImageFormat.JPEG => 
                JpegMetadataExtractor.ReadAIMetadata(imagePath),
            ImageTypeDetector.ImageFormat.WebP => 
                WebPMetadataExtractor.ReadAIMetadata(imagePath),
            _ => new AIMetadata()
        };
    }
}
```

### 预期发展（将来可能支持 AVIF、HEIC 等）
**升级为：Factory Method**

```csharp
// ✅ 易于添加新格式
public abstract class MetadataExtractor
{
    public abstract AIMetadata Read(string imagePath);
    
    public static MetadataExtractor Create(string imagePath)
    {
        var format = ImageTypeDetector.DetectImageFormat(imagePath);
        return format switch
        {
            /* ... */
        };
    }
}

// 新增 AVIF 支持
public class AvifExtractor : MetadataExtractor
{
    public override AIMetadata Read(string imagePath)
        => AvifMetadataExtractor.ReadAIMetadata(imagePath);
}
```

### 如果需要插件系统
**升级为：Dictionary Registry**

```csharp
// ✅ 完全可配置
public static class MetadataFactory
{
    private static readonly Dictionary<string, Func<string, AIMetadata>> extractors = 
        InitializeExtractors();
    
    private static Dictionary<string, Func<string, AIMetadata>> InitializeExtractors()
    {
        return new()
        {
            { "PNG", PngMetadataExtractor.ReadAIMetadata },
            { "JPEG", JpegMetadataExtractor.ReadAIMetadata },
            { "WEBP", WebPMetadataExtractor.ReadAIMetadata }
            // 启动时从 plugins/ 目录动态加载更多
        };
    }
    
    public static void RegisterExtractor(string format, 
        Func<string, AIMetadata> extractor)
    {
        extractors[format] = extractor;
    }
}
```

---

## 📈 维护成本对比

添加第 10 个格式时的成本：

```
Simple Factory:
│ 改动文件数：1（工厂类）
│ 行数修改：+3 行
│ 编译时间：< 1 秒
└─ 总成本：🟢 极低

Factory Method:
│ 改动文件数：2（新类 + 工厂 switch）
│ 行数修改：+15 行
│ 编译时间：< 2 秒
└─ 总成本：🟢 较低

Dictionary Registry:
│ 改动文件数：1（配置文件或注册代码）
│ 行数修改：+1 行
│ 编译时间：0 秒（可能不需要重新编译）
└─ 总成本：🟢 最低

Abstract Factory:
│ 改动文件数：5（工厂 + 提取器 + 写入器 + 验证器 + 抽象工厂）
│ 行数修改：+50 行
│ 编译时间：< 3 秒
└─ 总成本：🟡 较高
```

---

## 🎯 最终结论

| 优先级 | 场景 | 推荐方案 |
|--------|------|----------|
| 1️⃣ | 项目小，格式少 | **Simple Factory** - 最简单 |
| 2️⃣ | 项目中等，会扩展 | **Factory Method** - 最平衡 ⭐ |
| 3️⃣ | 项目大，需要插件 | **Dictionary Registry** - 最灵活 |
| 4️⃣ | 企业级，需要产品族 | **Abstract Factory** - 最完整 |

对于你的图片元数据项目，**Factory Method 是最佳选择**，既不过度设计，也有足够的扩展性。

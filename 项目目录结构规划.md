# C# 项目目录结构规划指南

## 📂 核心文件夹功能说明

### Models 文件夹
**用途**：存放数据模型和业务实体

```
Models/
├── ConversionReportRow.cs      ← 转换报告行（单条记录）
├── ImageInfoModel.cs           ← 图片信息模型
├── OutputDirectoryMode.cs      ← 输出目录模式枚举
└── AIMetadata.cs               ← AI 元数据模型
```

**职责**：
- ✅ 仅包含属性定义（Properties）
- ✅ 数据验证逻辑（Validation）
- ✅ 简单的数据转换（Mapping）
- ❌ 不包含业务逻辑
- ❌ 不包含数据库访问
- ❌ 不包含 I/O 操作

**设计原则**：
- **纯数据容器** - Models 是"纯粹"的，只表示数据结构
- **可序列化** - 通常用于网络传输、文件保存、数据库映射
- **无依赖** - 不依赖其他业务类，可独立使用

**实例**：
```csharp
// ✅ 好的 Model
public class ConversionReportRow
{
    public string SourcePath { get; set; }
    public int? SourceWidth { get; set; }
    public bool Success { get; set; }
    public string? ErrorMessage { get; set; }
    
    // 简单的验证
    public bool IsValid() => !string.IsNullOrEmpty(SourcePath);
}

// ❌ 错误的 Model（混入业务逻辑）
public class ConversionReportRow
{
    public void SaveToDatabase() { }  // ❌ 不应该在 Model 中
    public void ConvertImage() { }     // ❌ 不应该在 Model 中
    public void SendEmail() { }        // ❌ 不应该在 Model 中
}
```

---

### Services 文件夹
**用途**：存放业务逻辑和工具方法

```
Services/
├── ConversionService.cs         ← 图片转换核心流程
├── ReportService.cs             ← 报告生成
├── FileTimeService.cs           ← 文件时间戳管理
├── FileBackupService.cs         ← 文件备份
├── CreationTimeService.cs       ← 文件创建时间（P/Invoke）
├── ImageTypeDetector.cs         ← 图片类型识别
├── ImageConverter.cs            ← 图片格式转换
├── ValidationService.cs         ← 转换结果验证
├── MetadataExtractorFactory.cs  ← 元数据提取工厂
├── MetadataService.cs           ← 元数据服务
├── PngMetadataExtractor.cs      ← PNG 元数据提取
├── JpegMetadataExtractor.cs     ← JPEG 元数据提取
├── WebPMetadataExtractor.cs     ← WebP 元数据提取
├── AIMetadataExtractor.cs       ← AI 元数据提取
├── FileScanner.cs               ← 文件扫描
└── [其他具体服务...]
```

**职责**：
- ✅ 业务逻辑实现
- ✅ 数据处理和转换
- ✅ 文件 I/O 操作
- ✅ 外部 API 调用
- ✅ 数据库操作
- ❌ 不处理 HTTP 请求
- ❌ 不处理 UI 展示

**设计原则**：
- **单一职责** - 每个 Service 只做一件事
- **无状态** - 通常是静态类或单例
- **可重用** - 可被多个地方调用
- **易于测试** - 业务逻辑清晰，易于单元测试

**实例**：
```csharp
// ✅ 好的 Service
public static class ImageConverter
{
    // 清晰的单一职责：只负责转换
    public static void ConvertPngToJpeg(string sourcePath, string destPath, int quality)
    {
        using var image = Image.Load(sourcePath);
        image.SaveAsJpeg(destPath, new JpegEncoder { Quality = quality });
    }
}

// ✅ 好的 Service（业务逻辑）
public static class ConversionService
{
    // 编排多个步骤的业务流程
    public static void ScanConvertAndReport(string folder)
    {
        var files = FileScanner.GetImageFiles(folder);  // 扫描
        var rows = GenerateConversionRows(folder, files);  // 转换
        ReportService.WriteConversionReport(rows);  // 报告
    }
}

// ❌ 错误的 Service（混入 Model 或 UI 逻辑）
public static class BadService
{
    public void ShowDialog() { }  // ❌ UI 逻辑不属于 Service
    public ConversionReportRow ToViewModel() { }  // ❌ 映射应该分离
}
```

---

## 📊 项目规模与目录结构对应

### 🔹 小型项目（< 20 个文件）

**特点**：
- 功能简单
- 代码量少
- 团队小（1-2 人）
- 快速原型

**目录结构**：
```
Project/
├── src/
│   ├── Program.cs
│   ├── Models/
│   │   └── [少数数据模型]
│   └── Services/
│       └── [少数业务类]
├── tests/
│   └── BasicTests.cs
└── README.md
```

**特点**：
- ✅ 结构简单
- ✅ 快速导航
- ✅ 易于理解
- ❌ 会快速变得混乱

**示例**：
```
ImageInfo-Simple/
├── src/
│   ├── Program.cs
│   ├── Models/
│   │   ├── ConversionReportRow.cs
│   │   └── AIMetadata.cs
│   └── Services/
│       ├── ImageConverter.cs
│       └── ReportService.cs
└── tests/
    └── ConverterTests.cs
```

---

### 🔹 中型项目（20-100 个文件）

**特点**：
- 功能完整
- 代码量中等
- 团队中等（3-5 人）
- 有明确的功能划分

**目录结构**（推荐！）：
```
Project/
├── src/
│   ├── ImageInfo/
│   │   ├── Program.cs
│   │   ├── Models/
│   │   │   ├── ConversionReportRow.cs
│   │   │   ├── ImageInfoModel.cs
│   │   │   ├── AIMetadata.cs
│   │   │   └── OutputDirectoryMode.cs
│   │   ├── Services/
│   │   │   ├── ConversionService.cs
│   │   │   ├── ReportService.cs
│   │   │   ├── FileTimeService.cs
│   │   │   ├── ImageConverter.cs
│   │   │   ├── ValidationService.cs
│   │   │   ├── ImageTypeDetector.cs
│   │   │   ├── MetadataExtractorFactory.cs
│   │   │   ├── PngMetadataExtractor.cs
│   │   │   ├── JpegMetadataExtractor.cs
│   │   │   └── WebPMetadataExtractor.cs
│   │   └── bin/ & obj/
│   ├── ImageInfo.Tests/
│   │   ├── ConvertersTests.cs
│   │   ├── ReportTests.cs
│   │   └── bin/ & obj/
├── docs/
│   ├── 项目章程.md
│   ├── 设计模式详解.md
│   └── 功能规范.md
├── ImageInfo.sln
├── README.md
└── .gitignore
```

**特点**：
- ✅ 清晰的模块划分
- ✅ 易于导航
- ✅ 易于团队协作
- ✅ 符合 Visual Studio 标准

**你的项目就是这种结构！**

---

### 🔹 大型项目（100+ 个文件）

**特点**：
- 功能复杂
- 代码量大
- 团队大（10+ 人）
- 多功能模块

**目录结构（按功能域）**：
```
Project/
├── src/
│   ├── ImageInfo.Core/              ← 核心库
│   │   ├── Models/
│   │   │   ├── Entities/            ← 数据库实体
│   │   │   ├── DTOs/                ← 数据传输对象
│   │   │   ├── ViewModels/          ← 视图模型
│   │   │   └── Enums/               ← 枚举定义
│   │   ├── Services/
│   │   │   ├── Interfaces/          ← 服务接口
│   │   │   ├── Implementations/     ← 具体实现
│   │   │   └── Extensions/          ← 扩展方法
│   │   ├── Repositories/            ← 数据访问
│   │   ├── Utilities/               ← 工具类
│   │   └── Constants/               ← 常量定义
│   │
│   ├── ImageInfo.API/               ← Web API 项目
│   │   ├── Controllers/
│   │   ├── Middlewares/
│   │   ├── Startup.cs
│   │   └── appsettings.json
│   │
│   ├── ImageInfo.UI/                ← UI 项目（如果有）
│   │   ├── Views/
│   │   ├── ViewModels/
│   │   └── Converters/
│   │
│   └── ImageInfo.CLI/               ← 命令行项目
│       ├── Commands/
│       └── Program.cs
│
├── tests/
│   ├── ImageInfo.Core.Tests/
│   ├── ImageInfo.API.Tests/
│   ├── ImageInfo.Integration.Tests/
│   └── ImageInfo.Performance.Tests/
│
├── docs/
│   ├── architecture/
│   ├── api/
│   ├── design-patterns/
│   └── deployment/
│
├── tools/
│   ├── scripts/
│   └── generators/
│
├── ImageInfo.sln
├── README.md
├── LICENSE
└── .gitignore
```

**特点**：
- ✅ 清晰的分层结构
- ✅ 便于大团队开发
- ✅ 易于版本管理
- ✅ 便于维护和扩展
- ❌ 有些复杂

---

## 🎯 Models vs Services 对比

| 方面 | Models | Services |
|------|--------|----------|
| **主要职责** | 数据结构 | 业务逻辑 |
| **包含内容** | 属性、验证 | 方法、流程 |
| **是否有状态** | 有（数据状态） | 通常无（无状态） |
| **是否有依赖** | 无依赖 | 可依赖其他 Services |
| **是否可序列化** | ✅ 通常可以 | ❌ 通常不能 |
| **何时修改** | 当数据结构变化 | 当业务逻辑变化 |
| **示例** | ConversionReportRow | ConversionService |

---

## 📈 项目演进路径

```
阶段 1: 小型项目
│   Models/
│   Services/
│   └─ 文件少，结构简单
│
    ↓ 项目增长

阶段 2: 中型项目（你的项目现在所处的阶段）
│   Models/
│   │   ├── ConversionReportRow.cs
│   │   ├── ImageInfoModel.cs
│   │   ├── AIMetadata.cs
│   │   └── OutputDirectoryMode.cs
│   │
│   Services/
│   │   ├── ConversionService.cs
│   │   ├── ReportService.cs
│   │   ├── ImageConverter.cs
│   │   ├── ValidationService.cs
│   │   ├── FileTimeService.cs
│   │   ├── MetadataExtractorFactory.cs
│   │   ├── PngMetadataExtractor.cs
│   │   ├── JpegMetadataExtractor.cs
│   │   └── WebPMetadataExtractor.cs
│   │
│   └─ 文件多了，需要更清晰的组织
│
    ↓ 继续增长

阶段 3: 大型项目
│   Core/
│   │   ├── Models/
│   │   │   ├── Entities/
│   │   │   ├── DTOs/
│   │   │   └── Enums/
│   │   └── Services/
│   │       ├── Interfaces/
│   │       ├── Implementations/
│   │       └── Extensions/
│   │
│   API/
│   Controllers/
│   │
│   UI/
│   Views/
│   │
│   └─ 多层次结构，不同功能域分离
```

---

## 💡 组织文件的最佳实践

### 命名规范

```
Models/
├── [实体名].cs              ← ConversionReportRow.cs
├── [实体名]ViewModel.cs     ← ConversionReportRowViewModel.cs (UI 使用)
└── [实体名]DTO.cs          ← ConversionReportRowDTO.cs (API 传输)

Services/
├── I[服务名]Service.cs      ← IImageConverterService.cs (接口)
├── [服务名]Service.cs       ← ImageConverterService.cs (实现)
└── [服务名]Helper.cs        ← ImageConverterHelper.cs (辅助)
```

### 分组逻辑

**按功能分组（推荐）**：
```
Services/
├── 转换相关/
│   ├── ConversionService.cs
│   └── ImageConverter.cs
├── 元数据相关/
│   ├── MetadataExtractorFactory.cs
│   ├── PngMetadataExtractor.cs
│   └── JpegMetadataExtractor.cs
├── 时间相关/
│   ├── FileTimeService.cs
│   └── CreationTimeService.cs
└── 验证相关/
    └── ValidationService.cs
```

**按模式分组（不推荐）**：
```
Services/
├── Factories/
│   └── MetadataExtractorFactory.cs
├── Extractors/
│   ├── PngMetadataExtractor.cs
│   ├── JpegMetadataExtractor.cs
│   └── WebPMetadataExtractor.cs
└── Converters/
    └── ImageConverter.cs
```

---

## 🎓 你的项目（ImageInfo）分析

**当前规模**：中型项目（约 60 个文件）

**当前结构**（优秀！）：
```
✅ src/ImageInfo/
    ├── Program.cs
    ├── Models/          ← 4 个数据模型
    └── Services/        ← 13 个业务服务
    
✅ tests/ImageInfo.Tests/
    ├── ConvertersTests.cs
    ├── ReportTests.cs
    └── ReadWriteValidateTests.cs
```

**建议改进**（可选）：

如果文件继续增加到 100+，可以考虑：

```
src/ImageInfo/
├── Models/
│   ├── Entities/          ← 数据实体
│   │   ├── ConversionReportRow.cs
│   │   ├── ImageInfoModel.cs
│   │   └── AIMetadata.cs
│   ├── Enums/             ← 枚举
│   │   └── OutputDirectoryMode.cs
│   └── ViewModels/        ← 视图模型（如果有 UI）
│
├── Services/
│   ├── Interfaces/        ← 服务接口
│   │   └── IConversionService.cs
│   ├── Image/             ← 图片相关
│   │   ├── ImageConverter.cs
│   │   └── ImageTypeDetector.cs
│   ├── Metadata/          ← 元数据相关
│   │   ├── MetadataExtractorFactory.cs
│   │   ├── PngMetadataExtractor.cs
│   │   ├── JpegMetadataExtractor.cs
│   │   └── WebPMetadataExtractor.cs
│   ├── File/              ← 文件相关
│   │   ├── FileTimeService.cs
│   │   ├── FileBackupService.cs
│   │   ├── CreationTimeService.cs
│   │   └── FileScanner.cs
│   ├── Report/            ← 报告相关
│   │   └── ReportService.cs
│   └── Validation/        ← 验证相关
│       └── ValidationService.cs
│
└── Utilities/             ← 工具类（可选）
    └── Constants.cs
```

**但目前不需要！** 等到真正需要时再重构。

---

## 📋 检查清单

### 创建新的 Service 时：

- ✅ 类放在 Services/ 文件夹
- ✅ 类名以 Service 结尾（如 ImageConverter**Service**）
- ✅ 尽可能使用静态方法（无状态）
- ✅ 添加 XML 注释说明类的职责
- ✅ 单一职责原则：只做一件事
- ✅ 编写相应的单元测试

### 创建新的 Model 时：

- ✅ 类放在 Models/ 文件夹
- ✅ 只包含属性和简单验证
- ✅ 不包含任何业务逻辑
- ✅ 不依赖其他类
- ✅ 如果需要映射，创建单独的 Mapper 类

### 代码组织时：

- ✅ 同一个文件一般只有一个类
- ✅ 相关的类放在同一个文件夹
- ✅ 使用 using 语句保持清晰
- ✅ 遵循 C# 命名规范（PascalCase）
- ✅ 定期审查，识别可以提取的公共逻辑

---

## 🎯 总结

| 问题 | 答案 |
|------|------|
| **Models 放什么？** | 数据结构、属性、简单验证 |
| **Services 放什么？** | 业务逻辑、数据处理、工具方法 |
| **你的项目规模？** | 中型项目（推荐当前结构） |
| **何时需要重构？** | 文件超过 100 个或需要多个功能域时 |
| **现在应该做什么？** | 保持当前结构，继续按 Models/Services 分类 |

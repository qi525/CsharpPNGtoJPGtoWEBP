# 功能22：选择性转换 - 详细规划【文件转换模式】

## ⚠️ 功能说明
**本功能为文件转换操作**，允许用户选择特定的转换类型进行图片格式转换（PNG→JPG、PNG→WEBP、JPG→WEBP），会产生新的转换后文件。使用前务必备份原始文件。

## 1. 需求概述

### 1.1 功能定义
提供交互式菜单，用户可选择需要的转换模式，灵活进行 PNG→JPG、PNG→WEBP 或 JPG→WEBP 的单一或组合转换。

### 1.2 工作流程
```
启动程序
  ↓
显示转换选项菜单：
  1. PNG → JPG
  2. PNG → WEBP
  3. JPG → WEBP
  4. PNG → JPG + WEBP
  5. 全部转换
  0. 退出
  ↓
用户选择选项
  ↓
[转换执行]
  - 扫描源文件
  - 创建输出目录
  - 执行选中的转换
  - 生成转换报告
  - 自动打开报告
  ↓
[完成] 询问是否继续其他转换
```

### 1.3 输入输出
**交互式输入：** 用户菜单选择
**输出：** 
- 转换后的图片文件（按选择的转换模式）
- 转换报告Excel文件
- 不修改源文件，安全可逆

---

## 2. 技术方案设计

### 2.1 转换模式定义

```csharp
public enum ConversionMode
{
    PngToJpg = 1,       // PNG → JPG
    PngToWebp = 2,      // PNG → WEBP
    JpgToWebp = 3,      // JPG → WEBP
    PngToAll = 4,       // PNG → JPG + WEBP
    AllFormats = 5,     // 全部转换（包括JPG→WEBP）
    Exit = 0
}

public class SelectiveConversionConfig
{
    public ConversionMode SelectedMode { get; set; }
    public int JpgQuality { get; set; } = 85;
    public int WebpQuality { get; set; } = 80;
    public bool PreserveMetadata { get; set; } = true;
    public int MaxThreads { get; set; } = 8;
    public int TimeoutMs { get; set; } = 30000;
}
```

### 2.2 转换参数与质量配置

```csharp
public class QualityProfile
{
    public static readonly QualityProfile High = new() 
    { 
        JpgQuality = 95, 
        WebpQuality = 90,
        Description = "高质量（文件较大）"
    };
    
    public static readonly QualityProfile Balanced = new() 
    { 
        JpgQuality = 85, 
        WebpQuality = 80,
        Description = "平衡（推荐）"
    };
    
    public static readonly QualityProfile Compressed = new() 
    { 
        JpgQuality = 75, 
        WebpQuality = 70,
        Description = "压缩（文件较小）"
    };
    
    public int JpgQuality { get; set; }
    public int WebpQuality { get; set; }
    public string Description { get; set; }
}
```

### 2.3 性能预期

```
单文件转换：100-500ms
全量数据转换时间（依赖选择）：
  - PNG→JPG 仅：~2-3 小时
  - PNG→WEBP 仅：~2-3 小时
  - JPG→WEBP 仅：~1-2 小时
  - 全部：~5-8 小时（并行）
  
交互响应：< 100ms
菜单显示：< 50ms
```

---

## 3. 实现流程详细设计

### 3.1 前置步骤：交互菜单

#### 第一步：显示主菜单 (DisplayMainMenu)

**过程：**
```
清屏并显示菜单：

┌─────────────────────────────────────┐
│  ImageInfo - 选择性转换             │
├─────────────────────────────────────┤
│  请选择转换类型：                   │
│                                     │
│  1. PNG → JPG                       │
│  2. PNG → WEBP                      │
│  3. JPG → WEBP                      │
│  4. PNG → JPG + WEBP                │
│  5. 全部转换                        │
│  0. 退出                            │
│                                     │
│  请输入选择 (0-5): _                │
└─────────────────────────────────────┘
  ↓
获取用户输入
  ↓
验证输入有效性
```

**伪代码：**
```csharp
public static ConversionMode DisplayMainMenu()
{
    while(true)
    {
        Console.Clear();
        Console.WriteLine("╔════════════════════════════════════════╗");
        Console.WriteLine("║  ImageInfo - 选择性图片转换            ║");
        Console.WriteLine("╠════════════════════════════════════════╣");
        Console.WriteLine("║  1. PNG → JPG                          ║");
        Console.WriteLine("║  2. PNG → WEBP                         ║");
        Console.WriteLine("║  3. JPG → WEBP                         ║");
        Console.WriteLine("║  4. PNG → JPG + WEBP                   ║");
        Console.WriteLine("║  5. 全部转换                           ║");
        Console.WriteLine("║  0. 退出                               ║");
        Console.WriteLine("╚════════════════════════════════════════╝");
        
        Console.Write("\n请输入选择 (0-5): ");
        if(int.TryParse(Console.ReadLine(), out int choice) && 
           choice >= 0 && choice <= 5)
        {
            return (ConversionMode)choice;
        }
        
        Console.WriteLine("❌ 输入无效，请重试");
        Thread.Sleep(1000);
    }
}
```

**复杂度：** O(1)（固定菜单）

---

#### 第二步：选择质量配置 (SelectQualityProfile)

**过程：**
```
显示质量选项：

高质量（推荐）  - JPG:95, WEBP:90
平衡（默认）    - JPG:85, WEBP:80
压缩            - JPG:75, WEBP:70
自定义          - 手动输入值

  ↓
获取用户选择
  ↓
返回配置
```

**伪代码：**
```csharp
public static QualityProfile SelectQualityProfile()
{
    Console.WriteLine("\n请选择质量级别：");
    Console.WriteLine("1. 高质量（推荐，文件较大）");
    Console.WriteLine("2. 平衡（默认）");
    Console.WriteLine("3. 压缩（文件较小）");
    Console.WriteLine("4. 自定义");
    
    var input = Console.ReadLine();
    
    return input switch
    {
        "1" => QualityProfile.High,
        "2" => QualityProfile.Balanced,
        "3" => QualityProfile.Compressed,
        "4" => PromptCustomQuality(),
        _ => QualityProfile.Balanced
    };
}
```

**复杂度：** O(1)

---

### 3.2 核心步骤：转换执行

#### 第三步：执行转换 (ExecuteSelectedConversion)

**输入：** `ConversionMode mode, SelectiveConversionConfig config`

**过程：**
```
根据选中的模式执行转换：

[模式1] PNG→JPG
  └─ 扫描PNG文件 → 转换 → 报告
  
[模式2] PNG→WEBP
  └─ 扫描PNG文件 → 转换 → 报告
  
[模式3] JPG→WEBP
  └─ 扫描JPG文件 → 转换 → 报告
  
[模式4] PNG→JPG+WEBP
  └─ 扫描PNG文件 → 转换(JPG) 并 转换(WEBP) → 报告
  
[模式5] 全部
  └─ 并行执行模式1,2,3 → 报告
```

**伪代码：**
```csharp
public static void ExecuteSelectedConversion(
    ConversionMode mode,
    SelectiveConversionConfig config,
    string sourceFolder)
{
    Console.WriteLine($"\n🔄 开始执行转换...\n");
    var stopwatch = Stopwatch.StartNew();
    
    var tasks = mode switch
    {
        ConversionMode.PngToJpg => new[] 
        { 
            Task.Run(() => ConvertPngToJpg(sourceFolder, config)) 
        },
        ConversionMode.PngToWebp => new[] 
        { 
            Task.Run(() => ConvertPngToWebp(sourceFolder, config)) 
        },
        ConversionMode.JpgToWebp => new[] 
        { 
            Task.Run(() => ConvertJpgToWebp(sourceFolder, config)) 
        },
        ConversionMode.PngToAll => new[] 
        { 
            Task.Run(() => ConvertPngToJpg(sourceFolder, config)),
            Task.Run(() => ConvertPngToWebp(sourceFolder, config))
        },
        ConversionMode.AllFormats => new[] 
        { 
            Task.Run(() => ConvertPngToJpg(sourceFolder, config)),
            Task.Run(() => ConvertPngToWebp(sourceFolder, config)),
            Task.Run(() => ConvertJpgToWebp(sourceFolder, config))
        },
        _ => throw new ArgumentException("Invalid mode")
    };
    
    Task.WaitAll(tasks);
    stopwatch.Stop();
    
    Console.WriteLine($"✅ 转换完成，总耗时：{stopwatch.Elapsed.TotalHours:F1} 小时");
}
```

**复杂度：** 取决于选择的模式

---

#### 第四步：生成报告 (GenerateSelectionReport)

**过程：**
```
生成Excel报告，包含：
  - 转换统计（成功/失败/跳过）
  - 文件详情列表
  - 文件大小对比（原始 vs 转换后）
  - 错误日志
```

**复杂度：** O(N)，N = 转换文件数

---

#### 第五步：打开报告 (OpenReport)

**过程：**
```
自动打开生成的Excel报告
  ↓
询问是否继续其他转换
```

**复杂度：** O(1)

---

### 3.3 主循环：交互式操作

#### 第六步：主交互循环 (RunInteractiveMode)

**伪代码：**
```csharp
public static int RunInteractiveMode(string folder)
{
    while(true)
    {
        // 显示菜单并获取选择
        var mode = DisplayMainMenu();
        
        if(mode == ConversionMode.Exit)
        {
            Console.WriteLine("👋 程序退出");
            break;
        }
        
        // 选择质量配置
        var profile = SelectQualityProfile();
        
        // 确认转换
        if(!ConfirmConversion(mode, profile))
            continue;
        
        // 执行转换
        var config = new SelectiveConversionConfig
        {
            SelectedMode = mode,
            JpgQuality = profile.JpgQuality,
            WebpQuality = profile.WebpQuality
        };
        
        ExecuteSelectedConversion(mode, config, folder);
        
        // 打开报告
        OpenGeneratedReport(folder, mode);
        
        // 询问是否继续
        if(!PromptContinue())
            break;
    }
    
    return 0;
}
```

**复杂度：** O(1) 每轮循环

---

## 4. 集成点设计

### 4.1 修改ProductionModeService

**修改位置：** `ProductionModeService.cs` → `RunInteractiveMode()`

现有的 `ProductionModeService.RunInteractiveMode()` 已支持此功能，无需额外修改。

### 4.2 关键方法列表

```csharp
public static class SelectiveConversionService
{
    // ===== 交互方法 =====
    public static ConversionMode DisplayMainMenu();
    public static QualityProfile SelectQualityProfile();
    public static bool ConfirmConversion(ConversionMode mode, QualityProfile profile);
    public static bool PromptContinue();
    
    // ===== 转换方法 =====
    public static void ExecuteSelectedConversion(
        ConversionMode mode,
        SelectiveConversionConfig config,
        string sourceFolder);
    
    // ===== 报告方法 =====
    public static void GenerateSelectionReport(
        List<ConversionTask> tasks,
        string mode,
        string outputFolder);
    
    public static void OpenGeneratedReport(
        string folder,
        ConversionMode mode);
}
```

---

## 5. 新增文件清单

### 5.1 SelectiveConversionService.cs

**位置：** `src/ImageInfo/Services/SelectiveConversionService.cs`

**代码行数估计：** 300-400行

---

## 6. 测试计划

### 6.1 单元测试

```csharp
public class SelectiveConversionTests
{
    // [1] 测试菜单显示
    [Fact]
    public void DisplayMenu_ShowsOptions_Correctly()
    {
        var output = CaptureConsoleOutput(() => 
        {
            SelectiveConversionService.DisplayMainMenu();
        });
        
        Assert.Contains("PNG → JPG", output);
        Assert.Contains("退出", output);
    }
    
    // [2] 测试转换模式选择
    [Theory]
    [InlineData(ConversionMode.PngToJpg)]
    [InlineData(ConversionMode.AllFormats)]
    public void ExecuteConversion_ValidMode_Succeeds(ConversionMode mode)
    {
        var config = new SelectiveConversionConfig { SelectedMode = mode };
        
        SelectiveConversionService.ExecuteSelectedConversion(mode, config, testFolder);
        
        Assert.True(AllTasksCompleted(mode));
    }
    
    // [3] 测试质量配置
    [Fact]
    public void SelectQualityProfile_HighProfile_ReturnsCorrectValues()
    {
        var profile = QualityProfile.High;
        
        Assert.Equal(95, profile.JpgQuality);
        Assert.Equal(90, profile.WebpQuality);
    }
}
```

**测试用例数：** 10+个

---

## 7. 流程总结

### 7.1 实现步骤清单

| 序号 | 步骤 | 函数数 | 复杂度 | 预期耗时 |
|-----|------|--------|--------|---------|
| 1 | 显示主菜单 | 1 | O(1) | < 50ms |
| 2 | 选择质量配置 | 1 | O(1) | < 100ms |
| 3 | 执行转换 | 1+ | O(N/P) | 1-8小时 |
| 4 | 生成报告 | 1 | O(N) | < 1分钟 |
| 5 | 打开报告 | 1 | O(1) | < 1秒 |
| 6 | 主交互循环 | 1 | O(∞) | 用户决定 |

**总步骤数：** 6步
**总函数数：** 4个交互 + 2个转换 + 2个报告 = 8个
**预期首轮耗时：** 1-8小时（依赖选择）

---

## 8. 用户交互流程示例

```
启动 dotnet run

╔════════════════════════════════════════╗
║  ImageInfo - 选择性图片转换            ║
╠════════════════════════════════════════╣
║  1. PNG → JPG                          ║
║  2. PNG → WEBP                         ║
║  3. JPG → WEBP                         ║
║  4. PNG → JPG + WEBP                   ║
║  5. 全部转换                           ║
║  0. 退出                               ║
╚════════════════════════════════════════╝

请输入选择 (0-5): 1

请选择质量级别：
1. 高质量（推荐，文件较大）
2. 平衡（默认）
3. 压缩（文件较小）
4. 自定义

请输入选择 (1-4): 2

确认开始转换 PNG → JPG（质量：85）? (Y/N): y

🔄 开始执行转换...

已扫描：12,345 个 PNG 文件
正在转换... [████████████████████] 100%

✅ 转换完成，共 12,345 个文件
   成功：12,320 个
   失败：25 个
   总耗时：2 小时 15 分钟

📊 报告已生成：convert_png2jpg_report.xlsx
✨ 自动打开报告...

是否继续其他转换? (Y/N): n

👋 程序退出
```

---

## 9. 验收标准

- ✅ 菜单显示正确，交互流畅
- ✅ 所有转换模式可选
- ✅ 质量配置灵活
- ✅ 转换成功率 ≥ 99%
- ✅ 报告生成正确
- ✅ 支持重复转换和模式切换
- ✅ 无源文件修改
- ✅ 错误处理完善
- ✅ 用户友好的交互界面
